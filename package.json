{
    "name": "clipcc-extension-shiki-neurons",
    "version": "1.p",
    "author": "SimonShiki",
    "scripts": {
        "build": "rimraf ./build && mkdirp build && rimraf ./dist && mkdirp dist && webpack --bail",
        "build:dist": "NODE_ENV=production pnpm run build"
    },
    "dependencies": {
        "clipcc-extension": "^0.1.9"
    },
    "devDependencies": {
        "copy-webpack-plugin": "^11.0.0",
        "mkdirp": "^1.0.4",
        "rimraf": "^3.0.2",
        "webpack": "^5.73.0",
        "webpack-cli": "^4.10.0",
        "zip-webpack-plugin": "^4.0.1"
    }
}